# Wayland mode (Often causes tab drag freeze in Hyprland)
# google-chrome-stable --ozone-platform-hint=auto

# X11/XWayland mode (Fixes the tab drag freeze bug, uncomment to use this instead)
google-chrome-stable --ozone-platform-hint=x11